#pragma once
#include <stdint.h>
#include "list.h"

// This was designed so it could take #define'd parameters (e.g. bitwidth)
// While we're just using the defaults,
// there's nothing wrong with setting up the include structure "as intended"
#include "velbox/box.h"
